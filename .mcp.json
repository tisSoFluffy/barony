{
  "mcpServers": {
    "godot": {
      "command": "npx",
      "args": ["-y", "@coding-solo/godot-mcp@0.1.1"],
      "env": {
        "GODOT_PATH": "${GODOT_PATH:-}",
        "DEBUG": "${GODOT_MCP_DEBUG:-false}"
      }
    }
  }
}
